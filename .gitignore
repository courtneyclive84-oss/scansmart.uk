# macOS
.DS_Store

# Cowork / editor artefacts
.claude/
.fuse_hidden*
*.bak

# Python build artefacts (from _internal/ scripts)
__pycache__/
*.pyc

# Security audit 11 Jun 2026: the repo IS the Cloudflare Pages deploy AND is public
# on GitHub, so anything tracked here is world-readable at scansmart.uk/<path> and on
# GitHub. _internal/ (build scripts, runbooks, datasets, logs) and workers/ (Worker
# source + deploy runbooks) are local-only working material — never track them.
# This supersedes the old per-file _internal ignores ("keep parsed JSON + scripts
# tracked"); the whole trees live on disk and in Time Machine/STICK1 backups instead.
_internal/
workers/
# Repo working-memory doc — internal ops detail; tracking it would publish it
# at scansmart.uk/CLAUDE.md on the public deploy.
CLAUDE.md
